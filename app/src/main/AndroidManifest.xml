<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- Entry point: MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Direct target activities for your buttons -->
        <activity android:name=".FruitsVegetablesActivity" android:exported="true" />
        <activity android:name=".MeatActivity" android:exported="true" />
        <activity android:name=".DairyActivity" android:exported="true" />
        <activity android:name=".BakeryActivity" android:exported="true" />
        <activity android:name=".GrainsActivity" android:exported="true" />
        <activity android:name=".QuantityDialogActivity" android:exported="true"/>

    </application>

</manifest>
